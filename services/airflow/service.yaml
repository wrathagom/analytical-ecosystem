name: Airflow
category: orchestration
port: "${AIRFLOW_PORT:-8080}"
url: "http://localhost:${AIRFLOW_PORT:-8080}"
credentials: "${AIRFLOW_USER:-admin}/${AIRFLOW_PASSWORD:-admin}"
startup_time: 60

healthcheck:
  type: http
  endpoint: "http://localhost:${AIRFLOW_PORT:-8080}/health"
