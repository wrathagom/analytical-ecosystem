name: JupyterLab
category: notebook
port: "${JUPYTER_PORT:-8888}"
url: "http://localhost:${JUPYTER_PORT:-8888}/lab?token=${JUPYTER_TOKEN:-ecosystem}"
credentials: "token: ${JUPYTER_TOKEN:-ecosystem}"

healthcheck:
  type: http
  endpoint: "http://localhost:${JUPYTER_PORT:-8888}/api"
