name: n8n
category: orchestration
port: "${N8N_PORT:-5678}"
url: "http://${N8N_HOST:-localhost}:${N8N_PORT:-5678}"
credentials: "${N8N_EMAIL:-admin@localhost} / ${N8N_PASSWORD:-ecosystem}"

healthcheck:
  type: http
  endpoint: "http://localhost:${N8N_PORT:-5678}/healthz"
