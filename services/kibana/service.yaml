name: Kibana
category: search
port: "${KIBANA_PORT:-5601}"
url: "http://localhost:${KIBANA_PORT:-5601}"
depends_on:
  - elasticsearch

healthcheck:
  type: http
  endpoint: "http://localhost:${KIBANA_PORT:-5601}/api/status"
