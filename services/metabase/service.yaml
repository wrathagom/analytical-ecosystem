name: Metabase
category: visualization
port: 3000
url: "http://localhost:3000"
credentials: "admin@localhost / ecosystem"
startup_time: 60

healthcheck:
  type: http
  endpoint: "http://localhost:3000/api/health"
