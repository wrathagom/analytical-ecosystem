name: Grafana
category: visualization
port: "${GRAFANA_PORT:-3001}"
url: "http://localhost:${GRAFANA_PORT:-3001}"
credentials: "${GRAFANA_USER:-admin}/${GRAFANA_PASSWORD:-admin}"

healthcheck:
  type: http
  endpoint: "http://localhost:${GRAFANA_PORT:-3001}/api/health"
