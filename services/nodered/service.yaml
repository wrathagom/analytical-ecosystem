name: Node-RED
category: orchestration
port: "${NODERED_PORT:-1880}"
url: "http://localhost:${NODERED_PORT:-1880}"
credentials: "admin / ecosystem"

healthcheck:
  type: http
  endpoint: "http://localhost:${NODERED_PORT:-1880}"
