# Analytical Ecosystem Configuration
# Generate a service-specific env file:
#   ./ecosystem env --profiles postgres,jupyter --output .env
# Or generate a full example for all services:
#   ./ecosystem env --output .env.example

# Shared settings (used across services)
TIMEZONE=America/New_York
